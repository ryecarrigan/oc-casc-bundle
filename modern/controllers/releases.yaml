removeStrategy:
  rbac: SYNC
  items: NONE

items:
  - kind: folder
    displayName: Releases
    name: releases
    description: 'Monthly releases from CloudBees'
    items:
      - kind: managedController
        displayName: "CloudBees CI - ${v2609a}"
        description: 'Controller running the latest release from CloudBees'
        name: v2609a
        configuration:
          kubernetes:
            domain: releases-v2609a
            image: "CloudBees CI - Managed Controller - ${v2609a}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: main/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2608}"
        description: 'Controller running the latest release from CloudBees'
        name: v2608
        configuration:
          kubernetes:
            domain: releases-v2608
            image: "CloudBees CI - Managed Controller - ${v2608}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: main/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2607}"
        description: 'Controller running the latest release from CloudBees'
        name: v2607
        configuration:
          kubernetes:
            domain: releases-v2607
            image: "CloudBees CI - Managed Controller - ${v2607}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: main/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2606}"
        description: 'Controller running the latest release from CloudBees'
        name: v2606
        configuration:
          kubernetes:
            domain: releases-v2606
            image: "CloudBees CI - Managed Controller - ${v2606}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: main/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2605}"
        description: 'Controller running the latest release from CloudBees'
        name: v2605
        configuration:
          kubernetes:
            domain: releases-v2605
            image: "CloudBees CI - Managed Controller - ${v2605}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.541.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2604}"
        description: 'Controller running the latest release from CloudBees'
        name: v2604
        configuration:
          kubernetes:
            domain: releases-v2604
            image: "CloudBees CI - Managed Controller - ${v2604}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.541.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2603}"
        description: 'Controller running the latest release from CloudBees'
        name: v2603
        configuration:
          kubernetes:
            domain: releases-v2603
            image: "CloudBees CI - Managed Controller - ${v2603}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.541.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2602}"
        description: 'Controller running the latest release from CloudBees'
        name: v2602
        configuration:
          kubernetes:
            domain: releases-v2602
            image: "CloudBees CI - Managed Controller - ${v2602}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.541.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2601}"
        description: 'Controller running the January 2026 release from CloudBees'
        name: v2601
        configuration:
          kubernetes:
            domain: releases-v2601
            image: "CloudBees CI - Managed Controller - ${v2601}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.541.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2512}"
        description: 'Controller running the December 2025 release from CloudBees'
        name: v2512
        configuration:
          kubernetes:
            domain: releases-v2512
            image: "CloudBees CI - Managed Controller - ${v2512}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.516.3/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2511}"
        description: 'Controller running the November 2025 release from CloudBees'
        name: v2511
        configuration:
          kubernetes:
            domain: releases-v2511
            image: "CloudBees CI - Managed Controller - ${v2511}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.516.3/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2510}"
        description: 'Controller running the October 2025 release from CloudBees'
        name: v2510
        configuration:
          kubernetes:
            domain: releases-v2510
            image: "CloudBees CI - Managed Controller - ${v2510}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.528.1/controller
      - kind: managedController
        displayName: "CloudBees CI - ${v2509}"
        description: 'Controller running the September 2025 release from CloudBees'
        name: v2509
        configuration:
          kubernetes:
            domain: releases-v2509
            image: "CloudBees CI - Managed Controller - ${v2509}"
            replication:
              config:
                maxReplicas: 2
                replicas: 1
                targetCPUUtilizationPercentage: 80
        properties:
          - configurationAsCode:
              bundle: release-2.516.3/controller
