removeStrategy:
  items: SYNC
  rbac: SYNC

items:
  - kind: managedController
    name: testing
    configuration:
      kubernetes:
        domain: testing
    properties:
      - configurationAsCode:
          bundle: main/folder-admin-example
      - optOutProperty:
          securityEnforcerOptOutMode:
            authorizationOptOutMode: {}
